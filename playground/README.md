# Nuxt Spec playground app

Created by `pnpm create nuxt@latest`.

Included as a testing ground for checking CLI scripts on fresh projects.

- `node ../bin/cli.js setup` - to test `setup` command behavior
- `node ../bin/cli.js update` - to test `update` command behavior

**Be sure to ALWAYS revert changes and NEVER commit them!**
